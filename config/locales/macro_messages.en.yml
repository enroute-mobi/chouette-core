en:
  macro_messages:
    default: "Message on line %{name}"
    search_no_results_for_filter: "No messages found"
  activerecord:
    models:
      macro/message:
        zero:  "messages"
        one:   "message"
        other: "messages"
    attributes:
      macro/message:
        criticity: Criticity
        message: Message
        source: Source
