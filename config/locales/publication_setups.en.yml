en:
  publication_setups:
    search_no_results: 'No publication'
    actions:
      index: Aggregated offer Publications Setup
      new: Add a Publication Setup
      edit: Edit Publication Setup
      destroy_confirm: "Are you sure you want destroy this Publication Setup ?"
      publish: "Publish now"
      show_publications: "Show publications"
    show:
      title: "%{name}"
    form:
      add_destination: Add an output
      sections:
        destinations: "Destinations"
        export: Export settings
  activerecord:
    models:
      publication_setup:
        zero:  "Publication Setup"
        one:   "Publication Setup"
        other: "Publication Setups"
    attributes:
      publication_setup:
        name: "Name"
        export_type: "Export Type"
        export_setup: "Export Settings"
        enabled: "Enabled"
        force_daily_publishing: "Daily publication"
        enable_cache: "Optimize with a cache (Experimental)"
        priority: "Priority"
        destinations: "Output"
    errors:
      models:
        publication_setups:
          attributes:
            destinations:
              too_short: Must have at least one destination
