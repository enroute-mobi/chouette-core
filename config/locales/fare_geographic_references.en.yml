en:
  activerecord:
    models:
      fare/geographic_reference:
        zero: Fare geographic reference
        one: Fare geographic reference
        other: Fare geographic reference
    attributes:
      fare/geographic_reference:
        short_name: Short name
        name: Name
        fare_zone_id: Fare zone
    errors:
      models:
        fare/geographic_reference:
          attributes:
            short_name:
              duplicate_values_in_fare_geographic_references: Duplicate value violates unique constraint

