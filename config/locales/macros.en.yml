en:
  macros:
    actions:
      duplicate: Duplicate
      send_to_top: Send to top
      move_up: Move up
      move_down: Move down
      send_to_bottom: Send to bottom
      delete: Delete
      restore: Restore
  activerecord:
    models:
      macro:
        zero: "Macros"
        one: "Macro"
        other: "Macros"
      macro/create_code:
        one: Create Code
      macro/dummy:
        one: Simulate Macro
      macro/associate_shape:
        one: Associate Shapes according Code 
      macro/update_stop_area_compass_bearing:
        one: Define Stop Area compass bearing from Shapes
      macro/create_stop_area_referents:
        one: Create Referent Stop Areas
      macro/associate_stop_area_referent:
        one: Associate Stop Areas with a Referent
      macro/create_shape:
        one: Create Shapes
      macro/define_postal_address:
        one: Define Postal Address
      macro/define_attribute_from_particulars:
        one: Define Referent attribute from Particulars value
      macro/compute_journey_pattern_durations:
        one: Define Journey Pattern durations from passing times
      macro/compute_journey_pattern_distances:
        one: Define Journey Pattern distances from Shape
      macro/associate_shape_according_waypoints:
        one: Associate Shapes according Stop Areas
      macro/update_attribute_from_referent_to_particulars:
        one: Update attribute from Referent
      macro/delete_vehicle_journeys:
        one: Delete all Vehicle Journeys
      macro/compute_service_counts:
        one: Compute service counts
    attributes:
      macro/base:
        name: Name
        comments: Comments
        options: Options
      macro/dummy:
        expected_result: Expected result
      macro/create_code:
        source_attribute: Source Attribute
        source_pattern: Source Pattern
        target_code_space: Target Code Space
        target_model: Target Model
        target_pattern: Target Pattern
        expected_result: Expected result
      macro/define_postal_address:
        target_model: Target Model
      macro/define_attribute_from_particulars:
        target_attribute: Attribute
      macro/update_attribute_from_referent_to_particulars:
        target_attribute: Attribut
        override_existing_value: Override existing value
  simple_form:
    placeholders:
      macro_base:
        name: Describe macro goal
