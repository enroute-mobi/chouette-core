en:
  control_messages:
    default: "Message on line %{name}"
    presence_code: "The %{target_model} %{name} hasn’t got the code space %{code_space_name}"
  activerecord:
    models:
      control/message:
        zero:  "messages"
        one:   "message"
        other: "messages"
    attributes:
      control/message:
        criticity: Criticity
        message: Message
        source: Source
