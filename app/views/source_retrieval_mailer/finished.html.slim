h2.mail-title
  == 'mailers.hello'.t

p== t('mailers.source_retrieval_mailer.finished.body', source_name: @operation.source.name, status: @operation.user_status.text)
p= mailer_button(t('mailers.source_retrieval_mailer.button'), workbench_source_url(@operation.workbench, @operation.source))
