en:
  control_messages:
    default: "Message on line %{name}"
    presence_code: "The %{target_model} %{name} hasn’t got the code space %{code_space_name}"
    presence_custom_field: "The %{target_model} %{name} hasn’t got the custom field %{custom_field}"
    code_format: "The %{target_model} %{name} hasn’t got the code space %{code_space_name} with the expected format %{expected_format}"
    model_status: "The %{target_model} %{name} should have the status %{expected_status}"
    service_count_trend: "An anomaly on the line %{line} at %{date}"
    speed: "Improper Speed with %{speed} km/h in Journey Pattern '%{journey_pattern_name}' between '%{departure_name}' and '%{arrival_name}' (%{position})"
    presence_associated_model: "Invalid count (%{count}) for %{name}"
    geographical_zone: "The %{target_model} %{name} is not located into the selected geographical area"
    expected_provider: "The %{target_model} %{name} is not the expect provider"
    passing_times_in_time_range: "The %{target_model} %{name} contains %{vehicle_journey_at_stop} not included in the expect time range"
  activerecord:
    models:
      control/message:
        zero:  "messages"
        one:   "message"
        other: "messages"
    attributes:
      control/message:
        criticity: Criticity
        message: Message
        source: Source
