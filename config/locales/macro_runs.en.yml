en:
  activerecord:
    models:
      macro/base/run:
        zero: "Macro"
        one: "Macro"
        other: "Macros"
      macro/create_code/run:
        one: Create code
      macro/dummy/run:
        one: Simulate a macro
      macro/associate_shape/run:
        one: Associate shapes
      macro/update_stop_area_compass_bearing/run:
        one: Define the orientation of stop areas according to the shapes
      macro/create_stop_area_referents/run:
        one: Create stop area referent
      macro/associate_stop_area_referent/run:
        one: Associate stop area referent
      macro/create_shape/run:
        one: Create Shapes
      macro/define_postal_address/run:
        one: Define Postal Address
      macro/define_attribute_from_particulars/run:
        one: Define Referent attribute from Particulars value
      macro/compute_journey_pattern_durations/run:
        one: Define Journey Pattern durations from passing times
      macro/compute_journey_pattern_distances/run:
        one: Define Journey Pattern distances from Shape
      macro/associate_shape_according_waypoints/run:
        one: Associate Shapes according Stop Areas
      macro/update_attribute_from_referent_to_particulars/run:
        one: Update attribute from Referent
      macro/delete_vehicle_journeys/run:
        one: Delete all Vehicle journeys
      macro/compute_service_counts/run:
        one: Compute service counts
      macro/associate_documents/run:
        one: Associate documents
      macro/create_code_from_sequence/run:
        one: Create code from sequence
      macro/create_code_from_uuid/run:
        one: Create Code from UUID
      macro/define_french_code_insee/run:
        one: Define French Code Insee
      macro/define_stop_area_transport_mode/run:
        one: Define Stop Area transport mode
      macro/force_attribute_value/run:
        one: Force attribute value
      macro/define_route_name/run:
        one: Define Route name
    attributes:
      macro/base/run:
        name: Name
        comments: Comment
        target_model: Target model
        options: Options
      macro/create_code/run:
        source_attribute: Source Attribute
        source_pattern: Source Pattern
        target_code_space: Target code space
        target_pattern: Target pattern
      macro/dummy/run:
        expected_result: Expected result
      macro/associate_documents/run:
        model_code_space: Model code space
        document_code_space: Document code space
      macro/create_code_from_sequence/run:
        sequence_id: Séquence
        code_space_id: Espace de code
      macro/create_code_from_uuid/run:
        code_space_id: Code Space
      macro/force_attribute_value/run:
        target_model: Target Model
        target_attribute: Attribute
        expected_value: Expected value
      macro/define_route_name/run:
        target_attribute: Attribute
        target_format: Format
