en:
  processing_rule/workbenches:
    index:
      title: Processing Rules
    show:
      title: "%{name}"
    new:
      title: "Create a Processing Rule"
    edit:
      title: "Edit %{name}"
    search_no_results: "No processing rule matches your search"
    actions:
      show: "Show this Processing Rule"
      new: "Create a Processing Rule"
      edit: "Edit this Processing Rule"
      destroy: "Delete this Processing Rule"
      destroy_confirm: "Are you sure you want to delete this Processing Rule ?"
    name: "%{operation_step} %{processable_type} %{processable_name}"

  processing_rule/workgroups:
    index:
      title: Workgroup Processing Rules
    show:
      title: "%{name}"
    new:
      title: "Create a Processing Rule"
    edit:
      title: "Edit %{name}"
    search_no_results: "No processing rule matches your search"
    actions:
      show: "Show this Processing Rule"
      new: "Create a Processing Rule"
      edit: "Edit this Processing Rule"
      destroy: "Delete this Processing Rule"

  activerecord:
    models:
      processing_rule/base:
        zero: Processings rule
        one: Processing rule
        other: Processing rules
      processing_rule/workbench:
        zero: Processings rule
        one: Processing rule
        other: Processing rules
      processing_rule/workgroup:
        zero: Processing rule
        one: Processing rule
        other: Processing rules
    attributes:
      processing_rule/base:
        processing_manager_class_name: Processing Type
        processable_type: Processing Type
        processable_id: Processing
        operation_step: Operation Step
      processing/workbench:
        required_tag_ids: Required tags
        required_tags: Required tags
        excluded_tag_ids: Excluded tags
        excluded_tags: Excluded tags
      processing_rule/workgroup:
        control_list_id: Control List
        control_list: Control List
        workgroup: Workgroup
        target_workbenches: Target workbench(es)
        excluded_workbenches: Excluded workbench(es)
    errors:
      models:
        processing_rule/workbench:
          attributes:
            required_tag_ids:
              required_and_excluded_tags_cannot_overlap: "Required and excluded tags cannot overlap"
            excluded_tag_ids:
              required_and_excluded_tags_cannot_overlap: "Required and excluded tags cannot overlap"

  activemodel:
    models:
      processing_rule/flamingo_validation_processing_setup: "Flamingo Validation"
    attributes:
      processing_rule/flamingo_validation_processing_setup:
        ruleset: "Ruleset"
        include_schema: "Include schema"
        schema_version: "Schema version"
        token: "Connection token"

  enumerize:
    processing_rule/base:
      processable_type:
        "Macro::List": "Macro List"
        "Control::List": Control List
      operation_step:
        before_import: Before import
        after_import: After import
        before_merge: Before merge
        after_merge: After merge
        after_aggregate: After aggregate

  simple_form:
    hints:
      processing_rule:
        target_workbenches: All workbenches are selected by default
