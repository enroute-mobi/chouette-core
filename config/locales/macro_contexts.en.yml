en:
  activerecord:
    models:
      macro:
        zero:  "Macro Contexts"
        one:   "Macro Context"
        other: "Macro Contexts"
      macro/context/saved_search/run: Saved Search
      macro/context/saved_search: Saved Search
      macro/context/transport_mode/run: Transport Mode
      macro/context/transport_mode: Transport Mode
    attributes:
      macro/context: &macro_context
        name: Name
        comments: Comments
      macro/context/transport_mode: &transport_mode
        transport_mode: Transport Mode
      macro/context/saved_search: &saved_search
        saved_search_id: Saved search
      macro/context/run:
        <<: *macro_context
      macro/context/transport_mode/run:
        <<: *transport_mode
      macro/context/saved_search/run:
        <<: *saved_search
