en:
  activerecord:
    models:
      tag:
        zero: "Tag"
        one: "Tags"
        other: "Tags"
    attributes:
      tag:
        name: Name
        color: Color
        description: Description
    errors:
      models:
        tag:
          duplicate_tags_in_tags: "The Tag \"%{tag}\" doesn't allow several values in the same model"
          duplicate_values_in_tags: Duplicate key value violates unique constraint
          blank:  The tag value can't be empty
  tags:
    actions:
      new: Add Tag
      edit: Edit Tag
      destroy: Destroy Tag
      destroy_confirm: "Are you sure you want destroy this Tag ?"
    index:
      title: Tags
    show:
      title: Tag %{name}
    edit:
      title: Edit Tag %{name}
    new:
      title: Create new Tag
    create:
      title: Create new Tag
    form:
      sections:
        identification: "Identification"