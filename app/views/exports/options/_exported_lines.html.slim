- decorated_export = @export.decorate
= form.input :exported_lines,
  include_blank: false,
  input_html: { \
    'x-model': '$store.export.exportedLines', \
    'x-ref': 'exportedLinesSelect', \
    'x-bind:disabled': "$store.export.isExport && $store.export.referentialId == ''", \
  }

template[x-if="$store.export.exportedLines == 'line_ids'"]
  div[
    x-data
    x-init="$store.export.initLineIdsSelect(#{decorated_export.line_ids_options})"
  ]
    = form.input :line_ids,
      as: :tom_select,
      collection: [],
      input_html: { multiple: true, style: 'width: 100%' },
      label_html: { class: 'hidden' },
      include_hidden: false


template[x-if="$store.export.exportedLines == 'company_ids'"]
  div[
    x-data
    x-init="$store.export.initCompanyIdsSelect(#{decorated_export.company_ids_options})"
  ]
    = form.input :company_ids,
      as: :tom_select,
      collection: [],
      input_html: { multiple: true, style: 'width: 100%' },
      label_html: { class: 'hidden' },
      include_hidden: false

template[x-if="$store.export.exportedLines == 'line_provider_ids'"]
  div[
    x-data
    x-init="$store.export.initLineProviderIdsSelect(#{decorated_export.line_provider_ids_options})"
  ]
    = form.input :line_provider_ids,
      as: :tom_select,
      collection: [],
      input_html: { multiple: true, style: 'width: 100%' },
      label_html: { class: 'hidden' },
      include_hidden: false
