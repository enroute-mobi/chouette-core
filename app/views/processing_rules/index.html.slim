- breadcrumb :processing_rules, @workbench

.page_content
  .container-fluid
    - if @processing_rules.any?
      .row
        .col-lg-12
          = table_builder_2 @processing_rules,
            [ \
              TableBuilderHelper::Column.new( \
                key: :operation_step, \
                attribute: -> (pr) { pr.operation_step.text }, \
                sortable: false \
              ), \
              TableBuilderHelper::Column.new( \
                key: :processing_type, \
                attribute: -> (pr) { pr.processing_type.text }, \
                sortable: false,\
              ), \
              TableBuilderHelper::Column.new( \
                key: :processing_id, \
                attribute: -> (pr) { pr.processing.name }, \
                link_to: -> (pr) { url_for([pr.processing.workbench, pr.processing]) }, \
                sortable: false,\
              ), \
            ],
            cls: 'table has-filter has-search'

          = new_pagination @processing_rules, 'pull-right'

    - unless @processing_rules.any?
      .row.mt-xs
        .col-lg-12
          = replacement_msg t('processing_rules.search_no_results')
