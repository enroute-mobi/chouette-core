en:
  flamingo/validation_setups:
    actions:
      new: "Add a Flamingo Validation Setup"
      destroy_confirm: "Are you sure you want destroy this Flamingo Validation Setup?"
    index:
      title: "Flamingo Validation Setups"
    show:
      title: "%{name}"
    edit:
      title: "Edit %{name}"
    form:
      sections:
        identification: "Identification"
        setup: "Setup"
      hints:
        ignored_schema_rules: "Comma-separated list of XSD rule identifiers to ignore during validation (e.g., xsd-1824,xsd-1877)"
    search_no_results: "No Flamingo Validation Setup matches your query"
  activerecord:
    models:
      flamingo/validation_setup:
        zero: "Flamingo Validation Setup"
        one: "Flamingo Validation Setup"
        other: "Flamingo Validation Setups"
    attributes:
      flamingo/validation_setup:
        name: "Name"
        ruleset: "Ruleset"
        include_schema: "Include schema"
        schema_version: "Schema version"
        ignored_schema_rules: "Ignored Schema Rules"
        token: "Connection token"
