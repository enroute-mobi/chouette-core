fr:
  import_resources:
    index:
      metrics: "%{error_count} errors, %{warning_count} warnings"
  activerecord:
    attributes:
      import/resource:
        name: "Fichier"
        status: "État"
