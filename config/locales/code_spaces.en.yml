en:
  activerecord:
    models:
      code_space:
        zero: "Code Space"
        one: "Code Spaces"
        other: "Code Spaces"
    attributes:
      code_space:
        short_name: Short Name
        name: Name
        description: Description
        allow_multiple_values: Allow multiple values per model
    errors:
      models:
        code:
          duplicate_code_spaces_in_codes: "The Code Space \"%{code_space}\" doesn't allow several values in the same model"
          duplicate_values_in_codes: Duplicate key value violates unique constraint
  code_spaces:
    actions:
      new: Add Code Space
    index:
      title: Code Spaces
    show:
      title: Code Space %{name}
    edit:
      title: Edit Code Space %{name}
    new:
      title: Create new Code Space
    create:
      title: Create new Code Space